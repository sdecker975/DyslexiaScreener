using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MenuButtonPositioning : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//if(name.Equals("StartButton"))
  //          transform.position = GameObject.Find("Start").transform.position;
  //      if (name.Equals("SettingsButton"))
  //          transform.position = GameObject.Find("Settings").transform.position;
  //      Vector3 pos = GetComponent<RectTransform>().position;
  //      pos.z = 0;
  //      GetComponent<RectTransform>().position = pos;
    }
	
	// Update is called once per frame
	void Update () {
		
	}
}
