using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class TestTextMeshObject : MonoBehaviour {

	// Use this for initialization
	void Start () {
        // transform.GetChild(0).GetComponent<TextMeshPro>().text = "Hello";
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
